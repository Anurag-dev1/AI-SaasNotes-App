{
  "name": "saas-notes-app",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:worker": "cd worker && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:worker\" \"npm run dev:client\"",
    "install:all": "npm install --prefix server && npm install --prefix worker && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
